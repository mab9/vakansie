import "../domainDoc.js"

/**
 * Interface for Ctrl that represents a calendar projector controller.
 *
 * @interface CalendarProjectorCtrl
 */

/**
 * Get the calendar data
 *
 * @function
 * @name CalendarProjectorCtrl#getCalendarData
 * @returns {Array<Array<Day>>} An array containing for each month of the year an an array that contains days.
 **/

/**
 * Second function
 *
 * @function
 * @name CalendarProjectorCtrl#getMouseDown
 * @returns {Attribute} A boolean attribute that indicated if the mouse is clicked down on the calendar.
 *
 **/

/**
 * Third function
 *
 * @function
 * @name CalendarProjectorCtrl#getSelectionCtrl
 * @returns {SelectionController} A boolean attribute that indicated if the mouse is clicked down on the calendar.
 **/
